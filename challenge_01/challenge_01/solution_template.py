def count_word_frequency(text):
    """
    Count the frequency of each word in a string.

    Args:
        text (str): Input string of words

    Returns:
        dict: Dictionary with words as keys, counts as values

    Example:
        >>> count_word_frequency("the cat sat on the mat the cat")
        {'the': 3, 'cat': 2, 'sat': 1, 'on': 1, 'mat': 1}
    """
    # Write your solution here
    pass
